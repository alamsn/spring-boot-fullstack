docker command

docker compose up -d
docker compose ps
docker exec -it postgres bash
psql -U alamsn

./mvnw clean package jib:build \
-Djib.from.auth.username=${bamboo.docker_hub_username} \
-Djib.from.auth.password=${bamboo.docker_hub_password}